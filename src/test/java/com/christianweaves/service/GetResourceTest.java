package com.christianweaves.service;

import java.io.IOException;

/*import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.junit.Before;
import org.junit.jupiter.api.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

import static org.hamcrest.CoreMatchers.is;
import static org.junit.Assert.assertThat;
import static org.mockito.Mockito.*;*/

public class GetResourceTest {
	/*
	 * @Mock private HttpServletResponse response;
	 * 
	 * private GetResource getResource = new GetResource();
	 * 
	 * @Before public void setUp() throws Exception {
	 * MockitoAnnotations.initMocks(this); }
	 * 
	 * @Test public void shouldReturnNothingWithBlankFilename() {
	 * 
	 * HttpServletRequest request = mock(HttpServletRequest.class);
	 * 
	 * when(request.getParameter("file")).thenReturn("");
	 * 
	 * try { getResource.doGet(request, response);
	 * 
	 * 
	 * 
	 * 
	 * } catch (ServletException | IOException e) { // TODO Auto-generated catch
	 * block e.printStackTrace(); } }
	 */
}
